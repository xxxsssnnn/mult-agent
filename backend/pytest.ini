[pytest]
# 标准 pytest 套件所在目录（与脚本式门禁 tests/test_*.py 隔离，互不干扰）
testpaths = tests/pytest_suite
# 异步测试无需逐个加 @pytest.mark.asyncio
asyncio_mode = auto
addopts = -q
python_files = test_*.py
python_functions = test_*
