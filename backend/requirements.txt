# -*- coding: utf-8 -*-
# 直接依赖声明（人类维护）。
# 可复现的精确版本（含全部传递依赖）见 requirements.lock —— CI 一律用锁文件安装。
# 仅本文件不足以复现环境，请勿单独用它做生产/CI 安装。

# Core Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
aiosqlite==0.22.1

# Validation & Settings
pydantic==2.5.3
pydantic-settings==2.1.0

# 依赖一致性关键约束（勿随意放开，否则 pip check 变红）
#   - langchain-core 0.1.x 要求 packaging>=23.2,<24
#   - chromadb 1.5.9 依赖 build；而 build>=1.5 又要求 packaging>=24
#   => 必须同时钉住 packaging<24 与 build<1.5，三方约束才自洽
packaging>=23.2,<24.0
build>=1.0,<1.5

# Authentication
python-jose[cryptography]==3.5.0  # 3.3.0 有算法混淆等 3 条 CVE；注意其仍硬依赖 ecdsa，升级 jose 不会带掉 ecdsa
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 与 bcrypt>=4.1 不兼容（72 字节自检崩溃），必须 <4.1
python-multipart==0.0.32  # 0.0.6 有 8 条 CVE（DoS / 请求走私）

# LangChain (AI/LLM) —— 钉死已验证版本，避免在 langchain-core<0.2 区间内漂移
langchain==0.1.20
langchain-core==0.1.53
langchain-community==0.0.38
langchain-openai==0.0.8
langgraph==0.0.51
langsmith==0.1.147

# RAG (Retrieval-Augmented Generation)
chromadb==1.5.9
tiktoken==0.14.0

# Task Queue (optional)
redis==5.0.1
celery==5.3.6

# Utilities
python-dotenv==1.2.3  # 1.0.0 有 1 条 CVE
structlog==23.2.0
httpx==0.28.1
email-validator==2.1.0

# 可选 / 暂未启用（当前代码未引用；如需启用，需同步更新 requirements.lock 并重跑门禁）
# faiss-cpu>=1.7.4,<2.0
# pypdf>=3.17.0,<4.0
# python-docx>=1.1.0,<2.0
