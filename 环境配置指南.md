# 环境配置快速指南

## 🎯 当前状态

您的系统目前缺少以下必需软件：
- ❌ Python 3.10+
- ❌ Node.js 18+
- ℹ️  Docker（可选，用于数据库服务）

---

## ⚡ 最快安装方案（使用 Winget）

Windows 11 自带 winget 包管理器，可以一键安装：

### 步骤1：打开PowerShell（管理员）

1. 按 `Win + X`
2. 选择 "Windows PowerShell (管理员)" 或 "终端(管理员)"

### 步骤2：安装Python

```powershell
winget install Python.Python.3.11 --silent --accept-package-agreements
```

### 步骤3：安装Node.js

```powershell
winget install OpenJS.NodeJS.LTS --silent --accept-package-agreements
```

### 步骤4：重启终端

**重要**: 关闭所有PowerShell窗口，重新打开一个新的

### 步骤5：验证安装

```powershell
python --version
node --version
```

如果看到版本号，说明安装成功！✅

---

## 📥 手动安装方案

如果winget不可用，请手动下载：

### 1. Python 3.11

**下载地址**: https://www.python.org/downloads/

**安装要点**:
- ⚠️ **务必勾选 "Add Python to PATH"**
- 选择 "Install Now"
- 等待安装完成

**验证**:
```powershell
python --version
# 应该显示: Python 3.11.x
```

### 2. Node.js LTS

**下载地址**: https://nodejs.org/

**安装要点**:
- 下载左侧的 LTS 版本（长期支持版）
- 使用默认选项安装
- 等待安装完成

**验证**:
```powershell
node --version
# 应该显示: v18.x.x 或 v20.x.x

npm --version
# 应该显示版本号
```

---

## 🚀 安装完成后的配置

### 后端设置

```powershell
# 1. 进入后端目录
cd D:\multi-agent\backend

# 2. 创建虚拟环境
python -m venv venv

# 3. 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 如果遇到执行策略错误，运行：
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force

# 4. 安装依赖
pip install -r requirements.txt

# 5. 复制环境变量文件
Copy-Item .env.example .env
```

### 前端设置

打开**新的**PowerShell窗口：

```powershell
# 1. 进入前端目录
cd D:\multi-agent\frontend

# 2. 安装依赖
npm install

# 如果网络慢，使用国内镜像：
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 🗄️ 启动数据库服务

### 方案A：使用Docker（推荐）

如果您安装了Docker Desktop：

```powershell
cd D:\multi-agent
docker compose up -d postgres redis chromadb
```

### 方案B：跳过数据库（开发测试）

如果只是测试，可以先不启动数据库，后端会使用默认配置运行。

---

## ▶️ 启动项目

### 终端1：启动后端

```powershell
cd D:\multi-agent\backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload
```

后端将运行在: http://localhost:8000  
API文档: http://localhost:8000/docs

### 终端2：启动前端

打开**新的**PowerShell窗口：

```powershell
cd D:\multi-agent\frontend
npm run dev
```

前端将运行在: http://localhost:3000

---

## ✅ 验证清单

安装完成后，逐项检查：

- [ ] `python --version` 显示 Python 3.10+
- [ ] `node --version` 显示 v18+
- [ ] `npm --version` 显示版本号
- [ ] 后端依赖已安装 (`pip install -r requirements.txt`)
- [ ] 前端依赖已安装 (`npm install`)
- [ ] 后端可以启动 (`uvicorn app.main:app --reload`)
- [ ] 前端可以启动 (`npm run dev`)
- [ ] 可以访问 http://localhost:8000/docs
- [ ] 可以访问 http://localhost:3000

---

## 🐛 常见问题

### Q1: PowerShell提示"无法加载文件，因为禁止运行脚本"

**解决**:
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
```

### Q2: python命令找不到

**原因**: Python未添加到PATH

**解决**:
1. 重新运行Python安装程序
2. 选择 "Modify"
3. 勾选 "Add Python to PATH"
4. 完成安装
5. **重启终端**

### Q3: npm install失败

**解决**:
```powershell
# 清除缓存
npm cache clean --force

# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### Q4: 端口被占用

**检查端口**:
```powershell
netstat -ano | findstr :8000
netstat -ano | findstr :3000
```

**解决**: 修改配置文件中的端口号

---

## 📚 更多帮助

- 详细文档: `SETUP_GUIDE.md`
- 项目说明: `README.md`
- 快速开始: `QUICKSTART.md`

---

## 💡 建议安装顺序

1. ✅ 先安装 Python 和 Node.js
2. ✅ 配置后端和前端依赖
3. ✅ 测试启动后端和前端
4. ⭐ （可选）安装Docker用于完整部署

按照这个顺序，您应该能在30分钟内完成环境配置！

祝您配置顺利！🚀
